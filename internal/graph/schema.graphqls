# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Mutation {
  transfer(srcAddress: String!, dstAddress: String!, amount: Int!): TransferResult!
}

type Query {
  canAfford(address: String!, amount: Int!): Boolean!
}

type TransferResult {
  balance: Int!
}